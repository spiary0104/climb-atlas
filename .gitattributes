# The match index is hashed byte-for-byte (index-meta.json sha256); never convert its line endings.
import/index/* -text
# Batch files (records, plan, report, batch.json) are compared/hashed byte-for-byte; never convert their line endings.
import/batches/** -text
