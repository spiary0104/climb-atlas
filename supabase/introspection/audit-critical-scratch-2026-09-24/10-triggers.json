[
 {
  "schema": "public",
  "table": "spots",
  "name": "spots_pin_community_submission",
  "enabled": "O",
  "definition": "CREATE TRIGGER spots_pin_community_submission BEFORE INSERT ON public.spots FOR EACH ROW EXECUTE FUNCTION pin_community_submission()"
 },
 {
  "schema": "public",
  "table": "spots",
  "name": "spots_touch_updated_at",
  "enabled": "O",
  "definition": "CREATE TRIGGER spots_touch_updated_at BEFORE UPDATE ON public.spots FOR EACH ROW EXECUTE FUNCTION touch_updated_at()"
 }
]