# Claude Code
.claude/worktrees/
.claude/settings.local.json

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Scratch npm installs (this project has no package.json / build step —
# see CLAUDE.md "Stack" — node_modules here is one-off verification
# tooling for a session, never a tracked dependency)
node_modules/

# Generated / scratch
.playwright-mcp/
supabase_seed_output.sql

# Local pre-change backups of the seed dataset (recovery also via git tag pre-id-reconciliation)
data/backups/
data/reconciliation/*/live-snapshot.json
data/reconciliation/*/proposed-id-map.json

# Supabase CLI local state (link info, version cache)
supabase/.temp/
supabase/.branches/
